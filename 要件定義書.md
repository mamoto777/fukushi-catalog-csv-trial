# 福祉用具カタログアプリ 案件版(自社データ検証機能) 要件定義書

**作成日**: 2026-07-18
**ステータス**: 確定(2026-07-18。公開設定は§5のとおり Public + GitHub Pages を採用)
**ベース**: `fukushi-福祉用具カタログアプリ案`(デモ版、GitHub: mamoto777/fukushi-catalog-demo)のコード一式をコピーして新設

---

## 0. 正の所在

- 本フォルダ(`fukushi-csv-import-案件版`)が**案件版の正**である
- デモ版フォルダは納品済み状態として凍結し、今後の改修は本フォルダでのみ行う
- デモ側でバグが見つかった場合は、どちらに反映するかを都度判断する(自動では同期しない)

## 1. 想定シナリオ(仮定)

デモを見た企業から「**自社の商品データを入れ込んでテストプレイしたい**」と依頼された、という仮定の案件。

依頼元の条件:

- **エンジニアがいない**(Node.js・Git等の開発ツールは一切使えない)
- 有料の Microsoft Copilot は利用中(Excel等のAIアシスト)。他のLLMサービスは契約なし
- スマホ/タブレットでのテストプレイを想定

## 2. 採用方針: ブラウザ内CSV読み込み(検討済み・決定)

検討した3案(A: CSV往復方式 / B: ブラウザ内CSV読み込み / C: GitHub Actions自動化)のうち、**案Bを採用**する。

理由:

1. 依頼元がExcelでCSVを作るだけで、開発側の介在なしに何度でもテストできる
2. 実データが**ブラウザの中にとどまり、リポジトリ・公開ページ・外部サーバーに一切載らない**
3. ビルド不要でデータ差し替えができる(エンジニア不在条件を満たす)

## 3. 機能要件

### 3-1. CSV読み込み機能(新規・中核)

- ホーム画面等に「自社データで試す」導線を追加する
- CSVファイル(既存ひな形 `data/products-template.csv` 準拠)を選択すると、**ブラウザ内で**パース・検証し、商品データを差し替える
- 検証ルールは既存 `scripts/csv2json.mjs` と同一(必須列・insurance値・タグが語彙内か・価格が正の整数か)。**検証ロジックはブラウザ側へ移植し、csv2json.mjsと二重管理にならない構成を設計時に検討する**
- エラー時: 行番号つきで全エラーを列挙して表示し、データは差し替えない(黙って通さない)
- 読み込んだデータは**メモリ上のみ**。保存しない(リロードでデモデータに戻る)。localStorage等への保存は行わない
  - ※「毎回読み込み直すのが手間」となる場合の保存オプションは設計時の検討事項とする(保存する場合も端末内のみ・外部送信なしを厳守)

### 3-2. 表示状態の明示

- 自社データ表示中は、全画面に「読み込みデータ表示中(この端末のみ)」のバッジ等を常時表示する
- デモデータ表示時は現行どおり免責フッター(仮想データである旨)を表示する

### 3-3. 既存機能

- 困りごとナビ・ジャンル一覧・商品リスト・詳細・マッチング・PWAは**デモ版のまま維持**する
- 読み込んだCSVのデータも既存のタグ語彙(§5-2語彙)に基づいてマッチングに乗る

### 3-4. 依頼元向け記入キット(同梱ドキュメント)

エンジニア不在の依頼元が自力でCSVを作れるよう、以下を整備する:

1. **記入手順書**(Excelでの開き方→記入→CSV保存(UTF-8)→アプリで読み込み、まで)
2. **タグ語彙一覧表**(場面×困りごとの表をExcelに貼れる形で)
3. **Copilot依頼文例**(「この商品説明に合うタグを次の一覧から2〜4個選んで」等、タグ付け・データ整形をCopilotに手伝わせる定型文)

## 4. 非機能・セキュリティ要件

- **外部通信ゼロを維持**(CSV読み込みは File API によるローカル読み込みのみ)
- **個人情報非収集を維持**(CSVの内容は商品データのみ。氏名等の列は設けない)
- 実在の商品・価格・取引先データを**リポジトリに含めない**(リポジトリ内は引き続き仮想100商品のみ)
- `dangerouslySetInnerHTML` 禁止・`npm audit` 0維持等、デモ版のルールを引き継ぐ
- CSVパースに外部パッケージを使う場合は audit 0 を確認のうえ採用(自前実装との比較を設計時に判断)

## 5. 公開・ホスティング(設計時に決定)

案Bでは実データがリポジトリに載らないため、公開設定は2択:

| 案 | 内容 | 備考 |
|---|---|---|
| Public + GitHub Pages(推奨) | デモ版と同じ構成。URLを渡すだけでテスト可能 | リポジトリ内は仮想データのみなので公開しても安全 |
| Private + 別ホスティング | GitHub Free では Private リポジトリの Pages 不可のため、別サービスが必要 | 依頼元がURL自体の秘匿を求める場合のみ |

※ 既存の `.github/workflows/deploy.yml` はPages用。採用案の決定後に調整する。

**決定(2026-07-18)**: **Public + GitHub Pages を採用**。案Bでは実データがリポジトリに載らないため公開して安全であり、コスト0円・デモ版と同構成で運用できるため。

## 6. スコープ外

- 実データを公開ページに常時掲載する運用(本採用フェーズで別途検討)
- サーバー・DB・ログイン・データのクラウド保存
- 商品比較機能(デモ版から引き続きPhase 2)
- CSV以外の形式(Excel直接読み込み等)

## 7. 完了条件(たたき台)

1. ひな形準拠のCSVをブラウザで読み込み、全画面(ナビ・リスト・詳細)が差し替えデータで動作する
2. 不正なCSV(必須列欠落・語彙外タグ・価格不正)が行番号つきエラーで拒否される
3. 読み込みデータがリロードで消え、localStorage・Cookie・外部送信のいずれにも痕跡がない
4. 記入キット3点(手順書・タグ語彙表・Copilot文例)が揃っている
5. `npm run build` 通過・Vitest 全パス・`npm audit` 0件
6. スマホ実機でCSV読み込み→テストプレイが完了できる

## 8. 次工程

1. 本要件定義の確定(ユーザーレビュー)
2. `/設計支援` で `docs/design.md` を案件版仕様に改訂
3. 実装(`/implement`)
