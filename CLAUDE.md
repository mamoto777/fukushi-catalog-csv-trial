# fukushi-csv-import-案件版

福祉用具カタログアプリの案件版。デモ版(`fukushi-福祉用具カタログアプリ案` / mamoto777/fukushi-catalog-demo)をベースに、依頼元(エンジニア不在の企業を仮定)が自社商品CSVをブラウザ内で読み込んでテストプレイできる機能を追加する。

要件定義: `要件定義書.md`(2026-07-18確定)
設計書: @docs/design.md(案件版仕様・確定。実装は設計書§9のタスク分解に従う)
かんたん版設計書: `docs/design-かんたん版.md`(Excel取り込み・2026-07-22確定。実装時に Read して同書§9のタスク分解に従う。常時ロードを軽く保つため @import にはしない)

## プロジェクト固有ルール

- 環境変数・APIキー・外部通信は一切使わない(`.env` が存在しないことが正)。CSV読み込みはブラウザのFile APIによる端末内処理のみ
- `dangerouslySetInnerHTML` 使用禁止
- 商品タグは `src/data/vocab.json` の語彙のみ使用する(CSV読み込み時も同じ語彙で検証する)
- ビルド: `npm run build`(= `tsc --noEmit && vite build`)/ テスト: `npm test`
- **実在の商品・価格・取引先データをリポジトリに含めない**(リポジトリ内は仮想100商品のみ。実データはブラウザ内でのみ扱う)
- リポジトリ公開設定: **Public + GitHub Pages で確定**(2026-07-18)。リポジトリはGitHub.comで人間が手動作成する(設計書§8-2)
- 本フォルダが案件版の正。デモ版フォルダは凍結(改修はこちらでのみ行う)
